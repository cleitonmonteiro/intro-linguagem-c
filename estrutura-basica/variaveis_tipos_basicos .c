#include <stdio.h>
#include <stdlib.h>

int main() {
    
    int     a; // variavel para valores inteiros
    char    k; // variavel para caracteres
    float   g; // variavel para valores reais
    double  i; // variavel para valores reais, porem com dupla precisao nas casas decimais

    a = 10;  // atribuindo o valor 10 a a varialvel a

    /**
     * Atribuindo o caractere 'F' a varialvel k.
     * Obs : perceba as aspas simples.
     * Obs : o tipo char é para apenas um caracactere.
    */
    k = 'F';

    /**
     * Atribuindo valores reais as variaveis g e i
     * Obs : perceba que usamos '.' ao inver de ',' para numeros reais.
    */
    g = 12.1;
    i = 12.1;
    return 0;
}